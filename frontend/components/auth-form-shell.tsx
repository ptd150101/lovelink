import Link from "next/link";export function AuthFormShell({title,subtitle,children,footer}:{title:string;subtitle:string;children:React.ReactNode;footer?:React.ReactNode}){return <main className="auth-page"><Link href="/" className="brand auth-brand">LoveLink</Link><section className="auth-card"><h1>{title}</h1><p>{subtitle}</p>{children}{footer&&<div className="auth-footer">{footer}</div>}</section></main>}
