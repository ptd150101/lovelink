"use client";import {useEffect,useState} from "react";import {api} from "@/lib/api";import type {Profile} from "@/lib/types";import {ProfileCard} from "@/components/profile-card";import {Button,Empty,Field,Input,Select} from "@/components/ui";
type Ref={provinces:{code:string;name:string}[];occupations:{id:string;name:string}[];choices:Record<string,[string,string][]>};
export default function Discover(){const [items,setItems]=useState<Profile[]>([]);const [ref,setRef]=useState<Ref|null>(null);const [loading,setLoading]=useState(true);const [filters,setFilters]=useState<any>({min_age:18,max_age:40,min_height:"",max_height:"",province:"",hometown:"",occupation:"",education:"",income:"",goal:"",verified:false,sort:"recommended"});async function load(f=filters){setLoading(true);const q=new URLSearchParams();Object.entries(f).forEach(([k,v])=>{if(v!==""&&v!==false)q.set(k,String(v))});try{const r=await api<any>(`/discover?${q}`);setItems(r.results||r)}finally{setLoading(false)}}useEffect(()=>{api<Ref>("/reference-data").then(setRef);load()},[]);const set=(k:string,v:any)=>setFilters((x:any)=>({...x,[k]:v}));return <div className="page wide"><div className="page-heading"><div><span className="eyebrow">Khám phá</span><h1>Tìm người phù hợp</h1></div><Select value={filters.sort} onChange={e=>{set("sort",e.target.value);load({...filters,sort:e.target.value})}}><option value="recommended">Phù hợp nhất</option><option value="recent">Mới hoạt động</option><option value="newest">Hồ sơ mới</option><option value="age_asc">Tuổi tăng dần</option></Select></div><div className="discover-layout"><aside className="filter-panel"><h2>Bộ lọc</h2><div className="two"><Field label="Tuổi từ"><Input type="number" min={18} max={99} value={filters.min_age} onChange={e=>set("min_age",e.target.value)}/></Field><Field label="Đến"><Input type="number" min={18} max={99} value={filters.max_age} onChange={e=>set("max_age",e.target.value)}/></Field></div><div className="two"><Field label="Cao từ"><Input type="number" value={filters.min_height} onChange={e=>set("min_height",e.target.value)}/></Field><Field label="Đến"><Input type="number" value={filters.max_height} onChange={e=>set("max_height",e.target.value)}/></Field></div>{ref&&<><Field label="Nơi ở"><Select value={filters.province} onChange={e=>set("province",e.target.value)}><option value="">Tất cả</option>{ref.provinces.map(x=><option key={x.code} value={x.code}>{x.name}</option>)}</Select></Field><Field label="Quê quán"><Select value={filters.hometown} onChange={e=>set("hometown",e.target.value)}><option value="">Tất cả</option>{ref.provinces.map(x=><option key={x.code} value={x.code}>{x.name}</option>)}</Select></Field><Field label="Nghề nghiệp"><Select value={filters.occupation} onChange={e=>set("occupation",e.target.value)}><option value="">Tất cả</option>{ref.occupations.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</Select></Field><Field label="Học vấn"><Select value={filters.education} onChange={e=>set("education",e.target.value)}><option value="">Tất cả</option>{ref.choices.education.map(x=><option key={x[0]} value={x[0]}>{x[1]}</option>)}</Select></Field><Field label="Thu nhập"><Select value={filters.income} onChange={e=>set("income",e.target.value)}><option value="">Tất cả</option>{ref.choices.income.map(x=><option key={x[0]} value={x[0]}>{x[1]}</option>)}</Select></Field><Field label="Mục tiêu"><Select value={filters.goal} onChange={e=>set("goal",e.target.value)}><option value="">Tất cả</option>{ref.choices.goals.map(x=><option key={x[0]} value={x[0]}>{x[1]}</option>)}</Select></Field></>}<label className="check"><input type="checkbox" checked={filters.verified} onChange={e=>set("verified",e.target.checked)}/> Chỉ hồ sơ tích xanh</label><Button className="full" onClick={()=>load()}>Áp dụng</Button><Button variant="ghost" className="full" onClick={()=>{const f={min_age:18,max_age:40,min_height:"",max_height:"",province:"",hometown:"",occupation:"",education:"",income:"",goal:"",verified:false,sort:"recommended"};setFilters(f);load(f)}}>Xóa bộ lọc</Button></aside><section>{loading?<div className="empty">Đang tải hồ sơ…</div>:items.length?<div className="profile-grid">{items.map(p=><ProfileCard key={p.public_id} profile={p}/>)}</div>:<Empty title="Chưa tìm thấy hồ sơ" body="Hãy thử nới lỏng một vài điều kiện lọc."/>}</section></div></div>}
