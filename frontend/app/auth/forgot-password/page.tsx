"use client";import {useState} from "react";import {api} from "@/lib/api";import {AuthFormShell} from "@/components/auth-form-shell";import {Button,Field,Input} from "@/components/ui";
export default function Forgot(){const [email,setEmail]=useState("");const [msg,setMsg]=useState("");async function submit(e:React.FormEvent){e.preventDefault();const r=await api<{detail:string}>("/auth/password/forgot",{method:"POST",body:JSON.stringify({email})});setMsg(r.detail)}return <AuthFormShell title="Quên mật khẩu" subtitle="Nhập email để nhận liên kết đặt lại mật khẩu."><form onSubmit={submit} className="form-stack"><Field label="Email"><Input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></Field>{msg&&<div className="alert success-box">{msg}</div>}<Button>Gửi hướng dẫn</Button></form></AuthFormShell>}
