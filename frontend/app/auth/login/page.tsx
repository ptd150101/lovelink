"use client";import Link from "next/link";import {useSearchParams,useRouter} from "next/navigation";import {useState} from "react";import {api} from "@/lib/api";import {useAuth} from "@/components/auth-provider";import {AuthFormShell} from "@/components/auth-form-shell";import {Button,Field,Input} from "@/components/ui";
export default function Login(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [remember,setRemember]=useState(true);const [error,setError]=useState("");const [busy,setBusy]=useState(false);const {refresh}=useAuth();const router=useRouter();const q=useSearchParams();async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError("");try{await api("/auth/login",{method:"POST",body:JSON.stringify({email,password,remember})});await refresh();router.push(q.get("next")||"/discover")}catch(e:any){setError(e.message)}finally{setBusy(false)}}return <AuthFormShell title="Chào mừng trở lại" subtitle="Đăng nhập để tiếp tục kết nối." footer={<>Chưa có tài khoản? <Link href="/auth/register">Đăng ký ngay</Link></>}><form onSubmit={submit} className="form-stack"><Field label="Email"><Input type="email" value={email} onChange={e=>setEmail(e.target.value)} required autoComplete="email"/></Field><Field label="Mật khẩu"><Input type="password" value={password} onChange={e=>setPassword(e.target.value)} required autoComplete="current-password"/></Field><div className="row-between"><label className="check"><input type="checkbox" checked={remember} onChange={e=>setRemember(e.target.checked)}/> Ghi nhớ</label><Link href="/auth/forgot-password">Quên mật khẩu?</Link></div>{error&&<div className="alert error-box">{error}</div>}<Button disabled={busy}>{busy?"Đang đăng nhập…":"Đăng nhập"}</Button></form></AuthFormShell>}
