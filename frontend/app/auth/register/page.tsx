"use client";import Link from "next/link";import {useState} from "react";import {api} from "@/lib/api";import {AuthFormShell} from "@/components/auth-form-shell";import {Button,Field,Input} from "@/components/ui";
export default function Register(){const [data,setData]=useState({email:"",password:"",password_confirm:"",birth_date:"",accept_terms:false});const [message,setMessage]=useState("");const [error,setError]=useState("");const [busy,setBusy]=useState(false);const set=(k:string,v:any)=>setData(x=>({...x,[k]:v}));async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError("");try{await api("/auth/register",{method:"POST",body:JSON.stringify(data)});setMessage("Tài khoản đã được tạo. Hãy kiểm tra email để xác minh.")}catch(e:any){setError(e.message)}finally{setBusy(false)}}return <AuthFormShell title="Tạo tài khoản LoveLink" subtitle="Bạn phải từ 18 tuổi và đồng ý điều khoản sử dụng." footer={<>Đã có tài khoản? <Link href="/auth/login">Đăng nhập</Link></>}><form onSubmit={submit} className="form-stack"><Field label="Email"><Input type="email" value={data.email} onChange={e=>set("email",e.target.value)} required/></Field><Field label="Ngày sinh" hint="Ngày sinh đầy đủ không được công khai."><Input type="date" value={data.birth_date} onChange={e=>set("birth_date",e.target.value)} required/></Field><Field label="Mật khẩu" hint="Tối thiểu 10 ký tự."><Input type="password" value={data.password} onChange={e=>set("password",e.target.value)} required/></Field><Field label="Nhập lại mật khẩu"><Input type="password" value={data.password_confirm} onChange={e=>set("password_confirm",e.target.value)} required/></Field><label className="check"><input type="checkbox" checked={data.accept_terms} onChange={e=>set("accept_terms",e.target.checked)}/> Tôi đồng ý với <Link href="/terms">điều khoản</Link> và <Link href="/privacy">chính sách quyền riêng tư</Link>.</label>{error&&<div className="alert error-box">{error}</div>}{message&&<div className="alert success-box">{message}</div>}<Button disabled={busy}>{busy?"Đang tạo…":"Tạo tài khoản"}</Button></form></AuthFormShell>}
