"use client";import {useState} from "react";import {useAuth} from "@/components/auth-provider";import {SettingsNav} from "@/components/settings-nav";import {api} from "@/lib/api";import {Button,Card,Field,Input} from "@/components/ui";
export default function Account(){const {user}=useAuth();const [password,setPassword]=useState("");const [msg,setMsg]=useState("");async function remove(){if(!confirm("Hồ sơ sẽ bị ẩn ngay và tài khoản được xóa sau thời gian chờ. Tiếp tục?"))return;try{const r=await api<{detail:string}>("/auth/deletion-request",{method:"POST",body:JSON.stringify({password})});setMsg(r.detail)}catch(e:any){setMsg(e.message)}}return <div className="page narrow"><h1>Cài đặt</h1><SettingsNav/><Card><h2>Thông tin tài khoản</h2><dl className="detail-list"><div><dt>Email</dt><dd>{user?.email}</dd></div><div><dt>Trạng thái</dt><dd>{user?.status}</dd></div><div><dt>Xác minh email</dt><dd>{user?.is_email_verified?"Đã xác minh":"Chưa xác minh"}</dd></div></dl></Card><Card className="danger-zone"><h2>Xóa tài khoản</h2><p>Hồ sơ được ẩn ngay. Hệ thống giữ thời gian chờ trước khi ẩn danh dữ liệu để xử lý báo cáo và chống lạm dụng.</p><Field label="Nhập mật khẩu để xác nhận"><Input type="password" value={password} onChange={e=>setPassword(e.target.value)}/></Field><Button variant="danger" onClick={remove}>Yêu cầu xóa tài khoản</Button>{msg&&<p>{msg}</p>}</Card></div>}
