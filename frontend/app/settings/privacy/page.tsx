"use client";import {useEffect,useState} from "react";import {SettingsNav} from "@/components/settings-nav";import {api} from "@/lib/api";import {useRouter} from "next/navigation";import type {Profile} from "@/lib/types";import {Button,Card,Field,Select,Status} from "@/components/ui";
const fields:Record<string,string>={income_band:"Thu nhập",hometown_province:"Quê quán",current_province:"Nơi ở",education_level:"Học vấn",religion:"Tôn giáo",smoking_status:"Hút thuốc",drinking_status:"Uống rượu",children_status:"Con cái",children_plan:"Kế hoạch sinh con"};export default function Privacy(){const router=useRouter();const [p,setP]=useState<Profile|null>(null);const [msg,setMsg]=useState("");useEffect(()=>{api<Profile>("/me/profile").then(setP).catch(err=>{if((err as {status?:number}).status===401)router.push("/auth/login?next=/settings/privacy")})},[router]);async function save(){await api("/me/profile",{method:"PATCH",body:JSON.stringify({field_visibility:p?.field_visibility})});setMsg("Đã lưu quyền riêng tư.")}return <div className="page narrow"><h1>Cài đặt</h1><SettingsNav/><Card><h2>Quyền hiển thị</h2><p className="muted">Email, số điện thoại và ngày sinh đầy đủ không bao giờ được công khai.</p>{p&&Object.entries(fields).map(([k,v])=><Field key={k} label={v}><Select value={p.field_visibility?.[k]||"members"} onChange={e=>setP({...p,field_visibility:{...p.field_visibility,[k]:e.target.value}})}><option value="members">Thành viên đã đăng nhập</option><option value="connections">Chỉ người đã kết nối</option><option value="private">Chỉ mình tôi</option></Select></Field>)}<Button onClick={save}>Lưu thay đổi</Button>{msg&&<Status className="success-text">{msg}</Status>}</Card></div>}
